{
  "name": "tropy-plugin-autropy",
  "productName": "Autropy",
  "version": "0.1.0-alpha.3",
  "description": "Multimodal AI analysis for Tropy: suggests tags and descriptions with researcher review.",
  "author": "Anita Lucchesi",
  "homepage": "https://github.com/lucchesia/tropy-plugin-autropy",
  "main": "index.js",
  "scripts": {
    "build": "rollup -c rollup.config.mjs",
    "watch": "rollup -c rollup.config.mjs --watch",
    "test": "node --test \"test/*.test.mjs\""
  },
  "hooks": {
    "export": true
  },
  "options": [
    {
      "field": "model",
      "label": "Model ID (e.g. claude-sonnet-5, gemini-2.5-flash, gpt-4o)",
      "type": "string",
      "default": "",
      "required": true,
      "hint": "The provider's API identifier, not its display name — claude-sonnet-5, not \"Claude Sonnet 5\"."
    },
    {
      "field": "apiKey",
      "label": "API Key",
      "type": "password",
      "default": "",
      "required": true,
      "hint": "Stored as plain text in the plugins config.json."
    },
    {
      "field": "port",
      "label": "Tropy API Port",
      "type": "number",
      "default": 2029,
      "required": false
    },
    {
      "field": "suggestMetadata",
      "label": "Suggest Metadata",
      "type": "bool",
      "default": false,
      "required": false
    },
    {
      "field": "prompt",
      "label": "Custom Prompt (replaces analysis instructions; JSON output format is always enforced)",
      "type": "string",
      "default": "",
      "required": false
    },
    {
      "field": "outputLanguage",
      "label": "Output Language (e.g. Portuguese, French — leave blank for auto)",
      "type": "string",
      "default": "",
      "required": false
    }
  ],
  "devDependencies": {
    "@rollup/plugin-json": "^6.1.0",
    "@rollup/plugin-node-resolve": "^16.0.1",
    "rollup": "^4.0.0",
    "rollup-plugin-license": "^3.6.0"
  }
}
